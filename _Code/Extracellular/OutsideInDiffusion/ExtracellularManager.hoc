
{ load_file("ExtracellularManagerLoads.hoc") }

proc showExtracellularManagerBaseWidget() {

    dismissIfNotNil(manageAndRunWidget)
    manageAndRunWidget = new ManageAndRunWidget(3)
    manageAndRunWidget.show()
    manageAndRunWidget.showECSManagerHandler()
    manageAndRunWidget.showAltRunControlHandler()
}
