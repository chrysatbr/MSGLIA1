
// Sets initial params.
proc initParamsMain() {
    if (isGlialCell()) {
        // Both astrocyte and microglia use the same 2-argument format
        nanoBranchesManager.reseedBranches(createVecOfVals(nanoDistrLoaded, nanoDistrLoaded))
    }
    
    initVoltageAndRunControlParams()
}
