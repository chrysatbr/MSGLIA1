
// Sets initial params.
proc initParamsMain() {
    if (isAstrocyteOrNeuron) {
        nanoBranchesManager.reseedBranches(createVecOfVals(nanoDistrLoaded, nanoDistrLoaded))   // !! why here?
    }
    
    initVoltageAndRunControlParams()
}
